extern C fn write_char(u8) void;

fn println(string: ref [u8]) void {
    let mut i: usize = 0;
    while i < string.len {
        write_char(string[i]);
        i = i + 1;
    }
    write_char(10);
}

pub fn main() void {
    println(&#"Hello, World!"#);
}
