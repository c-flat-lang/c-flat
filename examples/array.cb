pub fn main() s32 {
    let mut a: [5; s32] = [0; 5];
    let mut b: [5; s32] = [0; 5];

    a[0] = 1;
    b[0] = 2;

    write_int(a[0]); // must be 1
    write_int(b[0]); // must be 2

    return 0;
}
