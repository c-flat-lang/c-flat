pub fn main() s32 {
    let mut array: [10; s32] = [ 0; 10 ];
    let index = 0;

    while index < array.len {
        let temp = array[index];
        array[index] = temp + 1;
        index = index + 1;
    }

    return array[0];
}
