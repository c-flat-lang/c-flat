extern C fn write_int(s32) void;
extern C fn write_char(u8) void;

// Item import: `add` is brought into scope and called directly.
use math::add;

pub fn main() s32 {
    let result = add(2, 3);
    write_int(result);
    write_char(10);
    return 0;
}
