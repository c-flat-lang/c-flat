public function main() s32 {
  %entry:
    @load u8 : start, 0
    @jump %loop

  %loop:
    @phi u8 : result, [(%entry, start), (%loop, next)]

    @cmp u8 : is_running, result, 10
    @add u8 : next, result, 1

    @jumpif is_running, %exit
    @jump %loop

  %exit:
    @ret u8 : result
}
